# Typora 1:1 复刻计划

> 目标：从零实现一个在**功能、交互、视觉**上与 Typora 一致的跨平台 Markdown 编辑器。
> Typora 本身闭源，本计划为**全新实现**（clean-room），不复制其任何源码。
> 本文只列「做什么 / 怎么做 / 什么顺序」，不含每步耗时。

---

## 0. 范围界定：什么叫「1:1」

复刻分三层，三层都要做才算 1:1：

| 层 | 含义 | 验收标准 |
|---|---|---|
| **行为层** | 每个功能的输入输出、快捷键、边角 case 与 Typora 一致 | 同样的 markdown，渲染结果、光标行为、导出产物一致 |
| **交互层** | 实时所见即所得（无分屏）、focus/typewriter/源码模式、侧边栏、设置面板 | 操作路径、动效、手感对齐 |
| **视觉层** | 排版、留白、内置主题、图标、设置界面布局 | 默认主题与各内置主题逐像素接近 |

> ⚠️ 法务提示：功能/交互/视觉可以复刻，但**软件名「Typora」、Logo、以及随包发布的官方主题 CSS 原文件**受商标/版权保护。1:1 复刻自用没问题，**若要分发**需换名换 Logo、主题用自己重写的等价实现。这一条在「分发」阶段处理。

---

## 1. 技术选型

| 关注点 | 选型 | 理由 |
|---|---|---|
| 桌面外壳 | **Electron** | Typora 自身就是 Electron，跨平台 + Chromium 排版引擎天然对齐 |
| 渲染层框架 | 原生 DOM / 轻量框架（不强依赖 React） | Typora 编辑区是直接操作 DOM 的 contenteditable，框架反而碍事 |
| **编辑器引擎** | **自研 WYSIWYG 引擎**（核心），底层可借鉴 ProseMirror 的文档模型思路 | 这是复刻的命门，详见 §3 |
| Markdown 解析 | **markdown-it**（可扩展、token 流清晰） | 插件生态全，方便对齐 Typora 的语法集 |
| 代码高亮 | **CodeMirror 6** | 代码块编辑 + 高亮，源码模式也复用 |
| 数学公式 | **KaTeX**（快）+ MathJax 兜底 | 行内 `$..$` / 块级 `$$..$$` |
| 图表 | **Mermaid** / flowchart.js / js-sequence-diagrams / Vega-Lite | 对齐 Typora 支持的全部图表类型 |
| 导入导出 | **Pandoc**（外部二进制，随包或检测） + Chromium 打印转 PDF | Typora 的多格式导出就是 Pandoc |
| 打包 | **electron-builder** | Win(nsis/msi) / macOS(dmg, 签名公证) / Linux(AppImage/deb) |
| 包管理 | **pnpm**（与本仓库现有约定一致） | — |
| 语言 | **TypeScript** 全量 | 引擎复杂，类型是刚需 |

---

## 2. 整体架构

```
┌─────────────────────────────────────────────┐
│  Main Process (Electron)                     │
│  ├─ 窗口/标签管理  ├─ 菜单/快捷键              │
│  ├─ 文件 IO / 监听 ├─ Pandoc 调用             │
│  └─ 设置持久化     └─ 自动更新                 │
└───────────────▲───────────────────────────────┘
                │ IPC (typed)
┌───────────────▼───────────────────────────────┐
│  Renderer (编辑窗口)                           │
│  ┌─────────┬──────────────────────┬─────────┐ │
│  │ 文件树   │   WYSIWYG 编辑区       │ 大纲     │ │
│  │ Sidebar │   (核心引擎)          │ Outline │ │
│  └─────────┴──────────────────────┴─────────┘ │
│  ├─ 主题系统(CSS)  ├─ 查找替换  ├─ 字数统计      │
│  └─ 设置面板        └─ 导出对话框                │
└───────────────────────────────────────────────┘
```

主进程管「系统级」的事，渲染进程是编辑器本体，二者用强类型 IPC 通信。

---

## 3. 核心：WYSIWYG 实时渲染引擎（命门）

Typora 最难复刻的就是这个——**在同一个 contenteditable 里，源码标记与渲染结果原地切换**，没有分屏。拆成四个子系统：

### 3.1 文档模型
- 把文档建模为**块（block）+ 行内（inline）**的树，而非纯字符串。
- 每个 block 知道自己的源 markdown 与渲染态 DOM。
- 编辑操作先改模型，再增量同步到 DOM（避免全量重渲染导致光标乱跳）。

### 3.2 光标所在块 = 源码态，其余块 = 渲染态
- 这是 Typora 的核心手感：光标进入某段落 → 该段落显示 markdown 标记（如 `**` `#`）；光标离开 → 标记隐藏、渲染为富文本。
- 需要精确的**光标 ↔ 源码偏移映射**，光标移动时切换相邻块的渲染/源码态。

### 3.3 输入即解析（增量）
- 监听 `beforeinput` / `input`，只重解析「当前块」，用 markdown-it 的 token 流更新该块 DOM。
- 处理触发式渲染：输入空格/回车时把 `# ` 变标题、`- ` 变列表、` ``` ` 变代码块、`|` 表格、`$$` 公式块等。

### 3.4 选区与命令系统
- 自实现选区保护（渲染态/源码态切换时维持选区）。
- 命令层：加粗/斜体/标题级别/列表/引用/缩进/链接/图片… 对齐 Typora 全部快捷键。

> 这一章是项目 60% 的工作量与全部风险所在，先做出可用原型再铺其他功能。

---

## 4. 完整功能清单（复刻 checklist）

### 4.1 块级元素
- [ ] 标题 H1–H6（含 `#` 与 Setext `===` 两种）
- [ ] 段落、软/硬换行
- [ ] 引用 blockquote（含嵌套）
- [ ] 无序/有序列表、**任务列表** `- [ ]`、多级嵌套
- [ ] 代码块（围栏 ``` 与缩进式）+ 语言高亮 + 复制按钮
- [ ] 表格（可视化编辑：增删行列、对齐、拖拽）
- [ ] 水平分割线
- [ ] 数学公式块 `$$`
- [ ] 图表块：Mermaid / 流程图 / 时序图 / Vega-Lite
- [ ] 脚注定义、目录 `[TOC]`
- [ ] YAML Front Matter
- [ ] 内联 HTML 块

### 4.2 行内元素
- [ ] 粗体 / 斜体 / 粗斜体 / 删除线
- [ ] 行内代码、行内公式 `$..$`
- [ ] 链接（行内式、引用式、自动链接）、图片
- [ ] 高亮 `==` 、上标 `^x^`、下标 `~x~`
- [ ] 脚注引用、Emoji `:smile:`
- [ ] 智能标点、自动配对括号/引号

### 4.3 编辑体验
- [ ] **大纲面板**（点击跳转、折叠）
- [ ] **文件树侧边栏**（打开文件夹、增删改、拖拽）
- [ ] **源码模式**切换（CodeMirror 全文 md）
- [ ] **专注模式** Focus Mode（高亮当前段，其余淡化）
- [ ] **打字机模式** Typewriter（当前行居中）
- [ ] 查找 / 替换（正则、大小写、全词）
- [ ] 字数/字符/行数统计
- [ ] 拼写检查
- [ ] 自动保存 + 文件外部变更监听与提示
- [ ] 多标签 / 多窗口

### 4.4 图片处理
- [ ] 拖拽 / 粘贴插入图片
- [ ] 图片存储策略：复制到本地指定目录 / 相对路径 / Base64 / 图床上传（PicGo 风格）
- [ ] 图片缩放、对齐

### 4.5 导入 / 导出（Pandoc）
- [ ] 导出 PDF（Chromium 打印管线，含页眉页脚、页边距设置）
- [ ] 导出 HTML（带样式 / 不带样式）
- [ ] 导出 Word(docx) / RTF / OpenOffice
- [ ] 导出 LaTeX / Epub / reStructuredText / MediaWiki / Textile
- [ ] 导入 docx / LaTeX 等（Pandoc 反向）
- [ ] 复制为 Markdown / HTML

### 4.6 主题与外观
- [ ] CSS 主题引擎（用户可放自定义主题）
- [ ] 复刻内置主题等价实现：默认 / Night / Github / Newsprint / Pixyll / Whitey
- [ ] 界面明暗主题
- [ ] 字体、行距、段距、宽度等排版设置

### 4.7 应用层
- [ ] 偏好设置面板（常规/外观/编辑/Markdown/图像/导出/快捷键）
- [ ] 完整快捷键体系（对齐 Typora 默认键位）+ 自定义
- [ ] 多语言 i18n（中/英起步）
- [ ] 应用菜单（文件/编辑/段落/格式/视图/主题/帮助）
- [ ] 最近文件、文档恢复
- [ ] 自动更新
- [ ] 三平台打包与（macOS）签名公证

---

## 5. 里程碑（按依赖排序，不含时间）

> 原则：**先打通最难的引擎，再铺功能，最后做平台化与打磨。** 每个里程碑结束有一个可运行、可演示的版本。

### M1 — 骨架跑通
Electron + pnpm + TS 工程搭好；一个空窗口，能开/存 `.md` 纯文本；主/渲染进程 IPC、菜单、设置持久化框架到位。

### M2 — WYSIWYG 引擎原型（项目核心）
实现 §3 的文档模型 + 「光标块源码态 / 其余块渲染态」机制。先支持：标题、段落、粗斜体、列表、引用、行内代码、链接。做到输入丝滑、光标稳定。**这是 go/no-go 节点。**

### M3 — 块级元素补全
代码块高亮、表格可视化编辑、围栏公式、水平线、任务列表、Front Matter、内联 HTML。

### M4 — 富内容
数学公式（KaTeX）、各类图表（Mermaid 等）、脚注、`[TOC]`、Emoji、高亮/上下标等扩展语法。

### M5 — 工作区与编辑体验
文件树侧边栏、大纲面板、源码模式、Focus/Typewriter 模式、查找替换、字数统计、多标签。

### M6 — 图片与导入导出
图片插入与存储策略、图床上传；集成 Pandoc 打通全格式导出 + PDF 打印管线。

### M7 — 主题与设置
CSS 主题引擎、内置主题等价实现、明暗界面、完整偏好设置面板、快捷键自定义、i18n。

### M8 — 平台化与打磨
三平台打包、macOS 签名公证、自动更新、大文档性能优化、稳定性与边角 case 回归、与 Typora 逐项对照验收。

> 依赖关系：M2 是一切的地基；M3/M4 依赖 M2；M5 可与 M3/M4 部分并行；M6/M7 依赖前面成果；M8 收尾。

---

## 6. 主要风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| **WYSIWYG 引擎手感** | 光标抖动、渲染/源码切换闪烁、中文输入法(IME)合成态处理 | M2 集中攻坚；IME 用 `compositionstart/end` 专门处理；增量更新不动光标所在节点 |
| **大文档性能** | 数千段落时全量渲染卡顿 | 块级增量渲染 + 视口虚拟化（仅渲染可视块） |
| **Pandoc 依赖** | 体积大、各平台路径差异 | 随包内置或首次检测下载；导出失败给清晰提示 |
| **导出保真** | PDF/Word 与屏幕显示一致 | PDF 走 Chromium 打印复用同一套 CSS；Word 接受 Pandoc 合理近似 |
| **边角语法对齐** | Typora 有不少非标准扩展语法 | 建语法对照测试集，逐条 diff |
| **法务（分发时）** | 名称/Logo/官方主题 | 分发前换名换标、主题重写 |

---

## 7. 建议目录结构

```
xuan-md/
├─ PLAN.md                  # 本文档
├─ package.json
├─ electron-builder.yml
├─ src/
│  ├─ main/                 # 主进程
│  │  ├─ window.ts
│  │  ├─ menu.ts
│  │  ├─ file-io.ts
│  │  ├─ pandoc.ts
│  │  ├─ export-pdf.ts
│  │  └─ settings.ts
│  ├─ preload/              # 安全桥接
│  ├─ renderer/
│  │  ├─ editor/            # ★ WYSIWYG 引擎（核心）
│  │  │  ├─ model/          # 文档模型
│  │  │  ├─ blocks/         # 各块级渲染器
│  │  │  ├─ inline/         # 行内渲染器
│  │  │  ├─ commands/       # 命令/快捷键
│  │  │  ├─ cursor/         # 光标与选区
│  │  │  └─ source-mode/    # 源码态切换
│  │  ├─ sidebar/           # 文件树
│  │  ├─ outline/           # 大纲
│  │  ├─ find-replace/
│  │  ├─ settings-ui/
│  │  └─ themes/            # CSS 主题
│  ├─ shared/               # 跨进程类型/常量/IPC 协议
│  └─ i18n/
├─ vendor/                  # pandoc 等外部二进制（可选）
└─ test/
   └─ markdown-corpus/      # 语法对照测试集
```

---

## 8. 下一步

1. 确认技术选型（尤其是「自研引擎 vs 直接用 Milkdown/ProseMirror 改」——前者最贴 Typora，后者省力但手感有取舍）。
2. 从 **M1 骨架** 开工，快速进入 **M2 引擎原型**（决定项目成败的节点）。
3. 同步建立 §7 的 `test/markdown-corpus`，让「与 Typora 对照」从第一天就可量化。
